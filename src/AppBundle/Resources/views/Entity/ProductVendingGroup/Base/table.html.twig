{# AppBundle/Resources/views/Entity/ProductVendingGroup/Base/table.html.twig #}
{% extends ( standalone is defined ) ? '::void.html.twig' : 'AppBundle:Entity/ProductVendingGroup/Base:list.html.twig' %}

{% block variable %}
    <div class="outer scrollable-holder">
        <div class="inner scrollable">

            {% if productVendingGroups|length %}
                <table>
                    <tr>

                        {% include 'AppBundle:Entity/ProductVendingGroup/Table:th.html.twig' %}

                        {% block action_items_th %}{% endblock %}

                    </tr>

                    {% for productVendingGroup in productVendingGroups %}

                        {% if is_granted(constant('AppBundle\\Security\\Authorization\\Voter\\ProductVendingGroupVoter::PRODUCT_VENDING_GROUP_READ'), productVendingGroup) %}
                            <tr>

                                {% include 'AppBundle:Entity/ProductVendingGroup/Table:tr.html.twig' with { 'productVendingGroup': productVendingGroup } only %}

                                {% block action_items_tr %}{% endblock %}

                            </tr>
                        {% endif %}

                    {% endfor %}

                </table>
            {% else %}
                <div class="empty-holder">
                    <h2>{{ 'dashboard.table.empty.title'|trans }}</h2>
                    <p>{{ 'dashboard.table.empty.text'|trans }}</p>
                </div>
            {% endif %}

        </div>
    </div>
    <div class="bottom-info-panel">
        {% block action_button %}{% endblock %}

        {% include 'AppBundle:Dashboard/Common:pagination.html.twig' only %}
    </div>
{% endblock %}