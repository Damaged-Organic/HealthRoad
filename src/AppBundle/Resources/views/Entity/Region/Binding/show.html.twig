{# AppBundle/Resources/views/Entity/Region/Binding/show.html.twig #}
{% extends 'AppBundle:Entity/Region/Base:table.html.twig' %}

{% block action_items_th %}
    <th colspan="2" class="double-small-fixed">
        <span>управление</span>
    </th>
{% endblock %}

{% block action_items_tr %}

    {% if is_granted(constant('AppBundle\\Security\\Authorization\\Voter\\RegionVoter::REGION_UPDATE'), region) %}

        {% if is_granted(constant('AppBundle\\Security\\Authorization\\Voter\\RegionVoter::REGION_BIND'), region) %}
            <td class="small-fixed edit-holder">
                <div class="btn-edit">
                    <a href="{{ path('region_update', { 'id': region.id }) }}" class="icon hrd-edit"></a>
                </div>
            </td>
            <td class="small-fixed delete-holder">
                <div class="btn-bind binded">
                    <a href="{{ path('region_unbind', { 'targetId': region.id, 'objectClass': class(object)|lower, 'objectId': object.id }) }}" class="icon hrd-bind"></a>
                </div>
            </td>
        {% else %}
            <td class="double-small-fixed edit-holder" colspan="2">
                <div class="btn-edit">
                    <a href="{{ path('region_update', { 'id': region.id }) }}" class="icon hrd-edit"></a>
                </div>
            </td>
        {% endif %}

    {% else %}
        <td class="double-small-fixed view-holder" colspan="2">
            <div class="btn-view">
                <a href="{{ path('region_read', { 'id': region.id }) }}" class="icon hrd-view"></a>
            </div>
        </td>
    {% endif %}

{% endblock %}

{% block action_button %}

    {% if is_granted(action.voter, object) %}
        <div id="action-holder">
            <div id="action-holder" class="manage-holder">
                <div class="btn-connect">
                    <a href="{{ path(action.path, { 'objectClass': class(object)|lower, 'objectId': object.id }) }}"><span class="icon hrd-bind"></span>Привязать</a>
                </div>
            </div>
        </div>
    {% endif %}

{% endblock %}