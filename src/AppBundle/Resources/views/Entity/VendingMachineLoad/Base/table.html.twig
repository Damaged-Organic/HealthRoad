{# AppBundle/Resources/views/Entity/VendingMachineLoad/Base/table.html.twig #}
{% extends ( standalone is defined ) ? '::void.html.twig' : 'AppBundle:Entity/VendingMachineLoad/Base:list.html.twig' %}

{% block variable %}
    <div class="outer scrollable-holder">
        <div class="inner scrollable">

            {% if vendingMachineLoads|length %}
                <table>
                    <tr>

                        {% include 'AppBundle:Entity/VendingMachineLoad/Table:th.html.twig' %}

                        {% block action_items_th %}{% endblock %}

                    </tr>

                    {% for vendingMachineLoad in vendingMachineLoads %}
                        <tr>

                            {% include 'AppBundle:Entity/VendingMachineLoad/Table:tr.html.twig' with { 'vendingMachineLoad': vendingMachineLoad } only %}

                            {% block action_items_tr %}{% endblock %}

                        </tr>
                    {% endfor %}

                </table>
            {% else %}
                <div class="empty-holder">
                    <h2>{{ 'dashboard.table.empty.title'|trans }}</h2>
                    <p>{{ 'dashboard.table.empty.text_sync'|trans }}</p>
                </div>
            {% endif %}

        </div>
    </div>
    <div class="bottom-info-panel">
        {% block action_button %}{% endblock %}

        {% include 'AppBundle:Dashboard/Common:pagination.html.twig' only %}
    </div>
{% endblock %}