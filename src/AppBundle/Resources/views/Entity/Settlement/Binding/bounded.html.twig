{# AppBundle/Resources/views/Entity/Settlement/Binding/bounded.html.twig #}
{% extends 'AppBundle:Entity/Settlement/Base:item.html.twig' %}

{% block active_settlement_update_bounded_school %}
    {% if objectClass is same as('school') %}
        {{ 'class="active"' }}
    {% endif %}
{% endblock %}

{% block action_search %}
    <div id="search-widget">
        {{ render(controller('AppBundle:Dashboard/CommonDashboard:search')) }}
    </div>
{% endblock %}

{% block variable %}

    {{ bounded|raw }}

{% endblock %}
